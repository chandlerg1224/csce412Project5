version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install"

  pre_build:
    commands:
      - echo "No pre-build commands"

  build:
    commands:
      - echo "Nothing to build"

  post_build:
    commands:
      - echo "No post-build commands"

artifacts:
  files:
    - '**/*'
  discard-paths: yes
